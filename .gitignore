node_modules/
build/
dist/
coverage/
.DS_Store
Thumbs.db
*.log
.env*
!.env.example
.vscode/
.idea/

# Config local de ferramentas (MCP aponta pro project_ref do Supabase pessoal do
# desenvolvedor — não deve ser compartilhado entre instalações white-label).
.mcp.json
# .claude/skills são symlinks pra .agents/skills/ (aqui viram cópias no Windows) —
# regenerados via `npx skills add supabase/agent-skills`, não precisam ser versionados.
.claude/

# Legado: persistência migrou para Supabase (Postgres + Storage). Não versionar mais.
data/database.json
data/database.json.backup
data/uploads/
